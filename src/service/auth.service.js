export function AuthLogin(data, requestDierect) {
  // console.info({ data, request });

  // useUserStore

  // post ke database

  // cek ke databse

  window.location.href = requestDierect;
}
export function AuthRegister(data, requestDierect) {
  // console.info({ data, request });

  // useUserStore

  // post ke database

  // cek ke databse

  window.location.href = requestDierect;
}
